# generate key
ssh-keygen -t ed25519 -C "your_email@example.com"

# from your root 
vim .profile

# paste there this lines of code 
if [ -z "$SSH_AGENT_PID" ]; then
    eval "$(ssh-agent -s)"
    ssh-add ~/.ssh/id_ed25519
fi
